<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Do not rewrite API endpoints (PHP) or built assets
  RewriteRule ^api/ - [L]
  RewriteRule ^assets/ - [L]

  # Legacy route: visitas escolares moved under bioparque
  RewriteRule ^visitas-escolares/?$ /bioparque/visitas-escolares [R=301,L]
  RewriteRule ^visitas-escolares/index\.html$ /bioparque/visitas-escolares [R=301,L]

  # Serve existing files/directories as-is
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback (React Router BrowserRouter)
  RewriteRule . /index.html [L]
</IfModule>


